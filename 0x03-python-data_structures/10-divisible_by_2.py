#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    my_bool_list = my_list[]
    if my_list:
        for num in my_list:
            my_bool_list.append(False if num % 2 else True)
        return my_bool_list
