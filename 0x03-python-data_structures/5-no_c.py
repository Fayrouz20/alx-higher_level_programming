#!/usr/bin/python3
def no_c(my_string):
    my_new_string = ""

    for item in range(len(my_string)):
        if (my_sring[item] != "c" or my_string[item] != "C"):
            my_new_string += my_string[i]

    return my_new_string
